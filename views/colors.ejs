<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Archivo:ital,wght@0,400;1,400;1,700&display=swap" rel="stylesheet">
    
    
    <title>Colors</title>
</head>
<body>
    <div class="container" min-height="470.875px">
        <h1 id="pageTitle">My Color Collection</h1>
        <p>Pick a color from picker and add to collection. Color value will be automatically added in the form.</p>
        <div class="container__items">
            <div class="container__items__left">
                <form action="/add-color" method="POST" class="add-color">
                    <label for="colorName">Color&hairsp;Name</label>
                    <input type="text" name="colorName" id="colorName" placeholder="any name" />
                    <label for="colorvalue">Color&hairsp;Value</label>
                    <input type="text" name="colorValue" id="colorValue" placeholder="hex value" />
                    <button type="submit" class="submit">Add to collection</button>
                </form>
              
               <div class="grid">
                <% for (let i in colors) { %>
                    <form action="/delete" method="POST">
                        <div class="grid__item">
                            <svg width="60" height="60" class="grid__item__box">
                                <rect width="60" height="60" style="fill:<%= colors[i].value %>; margin: 1rem" />
                            </svg>
                            <div class="grid__item__item">
                                <span><%= colors[i].color %></span>
                                <span><%= colors[i].value %></span>
                                <input type="hidden" value="<%= colors[i].value %>" name="colorValue"/>
                                <button type="submit" class="delete-btn">Delete</button>
                            </div>  
                        </div>
                    </form>    
                <% } %>
               </div>
            </div>
            
           <div id="picker" class="container__items__right"></div>
           <div>
               <p>Color value: <span id="info"></span></p>
            </div>
        </div>
        
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@jaames/iro@5"></script>
    <script src="/listeners.js"></script>
</body>
</html>